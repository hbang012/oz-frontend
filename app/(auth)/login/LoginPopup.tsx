'use client';

export default function LoginPopup() {
  return (
    <div className="fixed inset-0 flex items-center justify-center bg-[rgba(0,0,0,.6)]">
      <div className="bg-[#fff] rounded-[16px] w-[360px] p-[20px_0]">
        LoginPopup
      </div>
    </div>
  );
}
